version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies"
      - npm install

  pre_build:
    commands:
      - echo "Pre-build step"

  build:
    commands:
      - echo "Building the application"
      - npm run build --if-present
